<?xml version="1.0" encoding="utf-8" ?>
<!--For all properties see: http://developer.android.com/guide/topics/resources/menu-resource.html-->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
  <!-- namespace added as per 19-157 http://schemas.android.com/apk/res-auto (using support) -->
  
  <item android:id="@+id/action_add"
         app:showAsAction="always"
        android:text="Add"
        android:icon="@mipmap/ic_action_add_circle_outline"
   />
  <item android:id="@+id/action_subscribe"
       app:showAsAction="always"
      android:text="Subscribe"
      android:icon="@mipmap/ic_attach_money"
   />

  <!-- android:icon="@mipmap/ic_action_add_circle_outline" -->
  <!-- 7:45 19-157 actually using support libraries ur no? no error shown here -->
</menu>
<!-- use this resource for tabsToolbar -->
<!-- Code to implement into Activity:

Android.Widget.SearchView searchView;
public override bool OnCreateOptionsMenu(IMenu menu)
{
  this.MenuInflater.Inflate(Resource.Menu.tabsMenu, menu);

  var searchItem = menu.FindItem(Resource.Id.action_search);

  searchView = searchItem.ActionProvider.JavaCast<Android.Widget.SearchView>();

  searchView.QueryTextSubmit += (sender, args) =>
  {
    Toast.MakeText(this, "You searched: " + args.Query, ToastLength.Short).Show();

  };


  return base.OnCreateOptionsMenu(menu);
}

-->